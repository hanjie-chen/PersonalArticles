# cloudflare-cert

当我们的 domain 托管到 cloudflare 上面的时候，我们就可以白嫖 cloudflare 上面的证书了

点击 domian 然后侧边栏进入 `SSL/TLS --> Origin Server --> Create Certificate` 就可以白嫖 15 年的证书了 

<img src="./images/cf-free-cert.png" alt="cf free cert" style="zoom:50%;" />